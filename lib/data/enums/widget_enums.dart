enum AppTheme { system, dark, light }

enum CircularBorderRadiusSide { all, top, bottom }

enum AppChipType { filled, outlined, dotted }

enum AppChipTextColor {
  themeSupportColor,
  backgroundSupportColor,
  outlineColor,
}

enum ExploreViewArgsPageType {
  interest,
  available,
}

enum ProfileAddEditDreamType {
  colleges,
  countries,
  careers,
}
